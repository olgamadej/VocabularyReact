import React from "react";

const Tooltip = () => {
    return (
        <div className="tooltip">
            <p>Tip: Use Alt + e for é, Alt + a for à, and so on.</p>
        </div>
    );
};

export default Tooltip;